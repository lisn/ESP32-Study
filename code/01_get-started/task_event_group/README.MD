**事件组（Event Group）** 是 FreeRTOS 提供的一种同步机制，允许任务等待一个或多个事件的组合。它本质上是一个整数（通常为 32 位，具体位数由宏 `configUSE_16_BIT_TICKS` 决定），其中每一位（bit）可以独立表示一个事件是否发生（1 表示发生，0 表示未发生）。任务可以设置、清除某一位，也可以等待某些位满足特定条件（如“任意位为 1”或“所有位都为 1”）。
